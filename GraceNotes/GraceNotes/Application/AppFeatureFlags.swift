import Foundation

enum AppFeatureFlags {
    /// Central switch for user-visible Cloud AI affordances while the underlying implementation stays available.
    static var cloudAIUserFacingEnabled = false
}

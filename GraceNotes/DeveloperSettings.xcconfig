// Optional local overrides for developer-only build settings.
// Gitignored file lives next to this one: create `DeveloperSettings.local.xcconfig` when needed.

#include? "DeveloperSettings.local.xcconfig"

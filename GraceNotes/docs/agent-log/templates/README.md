# Templates

Templates are optional helpers to keep substance clear and handoffs consistent.

Copy the relevant template into an initiative folder and fill it in. Replace the `NNN-short-topic` placeholder in frontmatter with the real initiative id (see `.agents/skills/agent-log/SKILL.md`). For low-risk changes, a concise update with `Decision`, `Open Questions`, and `Next Owner` is enough.
